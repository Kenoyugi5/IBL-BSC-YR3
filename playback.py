idea = input("Enter text:")

# `loading = idea.replace("","...")` is replacing every empty space in the `idea` string with three
# dots (`...`) and assigning the resulting string to the variable `loading`.
loading = idea.replace("","...")
print(loading)